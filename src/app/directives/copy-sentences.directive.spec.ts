import { CopySentencesDirective } from './copy-sentences.directive';

describe('CopySentencesDirective', () => {
  it('should create an instance', () => {
    const directive = new CopySentencesDirective();
    expect(directive).toBeTruthy();
  });
});
