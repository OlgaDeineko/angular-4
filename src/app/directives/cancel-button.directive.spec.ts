import { CancelButtonDirective } from './cancel-button.directive';

describe('CancelButtonDirective', () => {
  it('should create an instance', () => {
    // const directive = new CancelButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
