import { ToFormOptionsPipe } from './to-form-options.pipe';

describe('ToFormOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToFormOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
