import { FormValidationErrorsPipe } from './form-validation-errors.pipe';

describe('FormValidationErrorsPipe', () => {
  it('create an instance', () => {
    // const pipe = new FormValidationErrorsPipe();
    // expect(pipe).toBeTruthy();
  });
});
